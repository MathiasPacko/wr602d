{% extends 'base.html.twig' %}

{% block title %}Modifier le contact - PDF Converter{% endblock %}

{% block body %}
<div class="page-header">
    <h1>Modifier le contact</h1>
    <p>Mettez à jour les informations du contact</p>
</div>

<div class="card" style="max-width: 520px;">
    <div class="flex gap-2 items-center mb-3" style="padding-bottom: 20px; border-bottom: 1px solid var(--border-subtle);">
        <div class="card-icon" style="margin-bottom: 0;">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/>
                <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/>
            </svg>
        </div>
        <div>
            <h3 style="font-size: 0.9375rem; font-weight: 600; margin-bottom: 2px;">Modifier le contact</h3>
            <p class="text-tertiary" style="font-size: 0.8125rem;">Mettez à jour les informations</p>
        </div>
    </div>

    {{ form_start(form, {'attr': {'data-turbo': 'false'}}) }}
        <div class="form-group">
            {{ form_label(form.firstname) }}
            {{ form_widget(form.firstname) }}
            {{ form_errors(form.firstname) }}
        </div>

        <div class="form-group">
            {{ form_label(form.lastname) }}
            {{ form_widget(form.lastname) }}
            {{ form_errors(form.lastname) }}
        </div>

        <div class="form-group">
            {{ form_label(form.email) }}
            {{ form_widget(form.email) }}
            {{ form_errors(form.email) }}
        </div>

        <div class="flex gap-1 mt-3">
            {{ form_widget(form.submit) }}
            <a href="{{ path('app_contacts') }}" class="btn btn-secondary">Annuler</a>
        </div>
    {{ form_end(form) }}
</div>
{% endblock %}
